# The brain is its own git repo, versioned independently of this tool.
# Anchored with a leading slash: an unanchored `brain/` also matches
# apps/brain/, which is this project's own source.
/brain/

.venv/
__pycache__/
*.py[cod]
.env
db.sqlite3
.DS_Store

# Playwright MCP scratch output
.playwright-mcp/
